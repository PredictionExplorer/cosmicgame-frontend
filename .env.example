# Required: no defaults for network or API/RPC URLs (avoids wrong backend/chain)
NEXT_PUBLIC_NETWORK=mainnet
# Must include the /api/cosmicgame path. Direct to Go example: http://127.0.0.1:8099/api/cosmicgame
NEXT_PUBLIC_API_URL=https://api.example.com/api/cosmicgame/
# Optional local same-origin proxy (avoids CORS if API is on another host/port). Uncomment both:
# NEXT_PUBLIC_API_URL=http://localhost:3000/api/cosmicgame
# COSMICGAME_API_UPSTREAM=http://127.0.0.1:8099
NEXT_PUBLIC_RPC_URL=https://arbitrum-mainnet.infura.io/v3/YOUR_KEY

# Optional: override block explorer base (no trailing slash). Default follows NEXT_PUBLIC_NETWORK
# (e.g. sepolia → https://sepolia.arbiscan.io, mainnet → https://arbiscan.io).
# NEXT_PUBLIC_EXPLORER_URL=https://sepolia.arbiscan.io

# Infura API key (only if using Infura RPC)
NEXT_PUBLIC_INFURA_KEY=

# WalletConnect/Reown Project ID (required by RainbowKit for QR and mobile wallet flows)
# - Create a free project at https://cloud.reown.com.
# - One project ID works for local dev, previews, and production.
# - The app will not boot without this value.
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=

# Optional
# NEXT_PUBLIC_GA4_MEASUREMENT_ID=
# NEXT_PUBLIC_SENTRY_DSN=
# Show git short SHA + branch in footer on Vercel Production too (hidden there by default).
# NEXT_PUBLIC_SHOW_BUILD_COMMIT=1
