/**
 * The imprint header sits in the hero's text column, beside the newest Random
 * Walk NFTs: the hero row draws the rule and the space under both, so the
 * header draws neither. Shared by the server page and the client fallback.
 */
export const IMPRINT_HEADER_CLASS = 'mb-0 border-b-0 pb-0 sm:mb-0 sm:pb-0';
