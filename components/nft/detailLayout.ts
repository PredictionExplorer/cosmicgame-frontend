/**
 * The Signature page's tracks: the plate, and from `lg` the wall label
 * beside it. The hero, the sections under it (traits, seed, histories) and
 * the loading skeleton all sit on these tracks, so every left and right edge
 * below the art lines up with the art.
 */
export const DETAIL_GRID_CLASS =
  'grid items-start gap-x-10 gap-y-8 lg:grid-cols-[minmax(0,1fr)_minmax(16rem,20rem)] xl:grid-cols-[minmax(0,1fr)_minmax(18rem,22rem)] xl:gap-x-16';
