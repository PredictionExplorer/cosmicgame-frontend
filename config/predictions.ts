/**
 * Chaos Zero — the prediction market built for Cosmic Signature.
 *
 * Each Cosmic Signature cycle opens one question on Chaos Zero: will this
 * cycle finalize with more gestures than the previous one? The market is
 * fully collateralized in CST and runs with no owner or admin keys.
 */
export const CHAOS_ZERO_PREDICTIONS_URL = 'https://chaoszero.com';
